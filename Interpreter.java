
public class Interpreter {
	
	//does the traversal of the parse tree
	//once it finds a large enough expression (in like stack form or whatever)
	//then it passes it to the reflection stuff.
	private Node root;
	
	public Interpreter(Node root) {
		this.root = root;
	}
	
	

	public static void main (String args[]) {
		
	}
}

